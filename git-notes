1. git clone url

2. git add files
3. git commit -m "message"

OR

2a. git commit -am "message"

git log

git checkout has-code

git switch branch_name

add some random text

git switch -c new branch

this line has been added on merge-conflict branch
